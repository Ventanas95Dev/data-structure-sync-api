{
  "name": "data-structure-sync-api",
  "version": "1.0.0",
  "description": "WebSocket API with MongoDB for data synchronization",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mongoose": "^7.4.1",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prettier": "^3.4.0"
  }
}
